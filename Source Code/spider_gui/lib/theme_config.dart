import 'package:flutter/material.dart';

class AppTheme {
  // اللون الأساسي الافتراضي (الأخضر الأصلي)
  static Color primaryColor = const Color(0xFF00FF41); // Matrix Green الأصلي
  
  // خلفية سوداء
  static Color backgroundColor = const Color(0xFF0F0F0F);
  
  // مفتاح لتفعيل وضع الألوان المتعددة
  static bool isMultiColorMode = false;
}
